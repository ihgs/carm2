groups:
  - 算数:
      color:
        background: blue
  - PG:
      color:
        background: red

nodes:
  - 足し算:
      group: 算数
  - 引き算:
      group: 算数
  - 割り算:
      group: 算数
  - 掛け算:
      group: 算数
  - 分数:
      group: 算数
  - 基礎PG構文:
      group: PG
  - rails:
      group: PG
  - ruby:
      group: PG
  - html:
      group: PG
  - docker:
      group: PG
  - go:
      group: PG
  - linux:
      group: PG

edges:
  - 引き算 -> 足し算
  - 割り算 -> 足し算
  - 掛け算 -> 足し算
  - 割り算 -> 掛け算
  - 分数 -> 割り算
  - ruby -> 基礎PG構文
  - rails -> ruby
  - rails -> html
  - rails -> rake
  - go -> 基礎PG構文
  - docker -> go
  - docker -> linux
