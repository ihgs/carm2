<p id="notice"><%= notice %></p>

<h1>Listing Students</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.name[:family_name] %> <%= student.name[:first_name] %></td>
        <td><%= student.age %></td>
        <td><%= link_to 'Show', [:admin, student] %></td>
        <td><%= link_to 'Edit', [:edit, :admin, student] %></td>
        <td><%= link_to 'Destroy', [:admin, student], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Student', new_admin_student_path %>
